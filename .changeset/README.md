# Changesets

Run `npx changeset` in any PR that changes the published package. Pick the
bump (patch / minor / major) and write one line for the changelog. The release
workflow turns merged changesets into a version PR, and merging that publishes
to npm and JSR.
